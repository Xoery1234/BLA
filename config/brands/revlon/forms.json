{
  "$schema": "Form submission adapter config — TD-4 §4.2. Per-brand form backend routing.",
  "$note": "Phase A placeholder. Webhook endpoint TBD pending Revlon ESP selection.",
  "adapter": "webhook",
  "config": {
    "endpoint": "{{REVLON_FORM_WEBHOOK_ENDPOINT}}",
    "headers": {
      "X-API-Key": "{{REVLON_FORM_WEBHOOK_KEY}}",
      "Content-Type": "application/json"
    },
    "timeout": 5000,
    "retries": 2
  },
  "successMessage": "Thanks for signing up! We'll be in touch soon.",
  "successRedirect": null,
  "rateLimit": {
    "maxPerMinute": 5,
    "maxPerSession": 10
  },
  "fields": {
    "defaults": {
      "r1b": {
        "fields": ["email"],
        "consentRequired": true,
        "consentLabel": "I agree to receive marketing communications from Revlon."
      },
      "r2a": {
        "fields": ["firstName", "email", "shadeFamily"],
        "consentRequired": true,
        "consentLabel": "I agree to receive marketing communications from Revlon. You can unsubscribe at any time."
      }
    }
  }
}
