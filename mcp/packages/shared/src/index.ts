/**
 * `@bla/shared` — Sprint 1 scaffold barrel.
 * Step 2 expands this with auth types, full error hierarchy, event
 * signature stub, model cache minimums, and Infisical path validator.
 */

export { NotImplementedError } from './errors.js';
