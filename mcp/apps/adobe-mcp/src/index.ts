/**
 * bla-adobe-mcp — Adobe services MCP (Workfront + EDS + DA.live).
 *
 * Spec: docs/mcp/adobe-mcp-spec.md
 * PRD:  docs/PRD-BLA-FLYWHEEL-CONNECTORS-v2.md §3.1 (r3 2026-04-19)
 *
 * Sprint 1 ships this stub. Sprint 2 replaces with real tool wiring
 * (Workfront task CRUD + mTLS webhook receiver, EDS publish via
 * admin.hlx.page, DA.live source API).
 */

import { NotImplementedError } from '@bla/shared';

throw new NotImplementedError(
  'bla-adobe-mcp entry point not implemented — see docs/mcp/adobe-mcp-spec.md',
);
